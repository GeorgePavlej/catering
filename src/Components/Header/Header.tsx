import { HeaderBody } from "../HeaderBody/HeaderBody";
import { Navigation } from "../Navigation/Navigation";

export const Header = () => {

  return (
    <header>
      <Navigation />
      <HeaderBody />
    </header>
  );
}; 